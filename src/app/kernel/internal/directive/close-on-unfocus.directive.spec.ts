import { CloseOnUnfocusDirective } from './close-on-unfocus.directive';

describe('CloseOnUnfocusDirective', () => {
  it('should create an instance', () => {
    const directive = new CloseOnUnfocusDirective();
    expect(directive).toBeTruthy();
  });
});
